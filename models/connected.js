
class Connected {
    constructor (userId, diagramId, socketId){
        this.userId = userId;
        this.diagramId  = diagramId;
        this.socketId = socketId;
    }
}

module.exports = Connected;